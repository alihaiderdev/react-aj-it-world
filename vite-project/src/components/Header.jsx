
const Header = (props) => {
    // console.log("props header", props);

    return (
        // "background-color: red; font-family: sans-serif;"
        <h1 style={{ backgroundColor: "cyan", fontFamily: "sans-serif" }}>This is our Header</h1>
    )
}
// dump components are those components which are not using any state or props
export default Header;